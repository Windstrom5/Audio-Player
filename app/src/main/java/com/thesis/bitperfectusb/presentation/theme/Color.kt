package com.thesis.bitperfectusb.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Audiophile-grade dark palette ──────────────────────────────────────────────
val SignalTeal      = Color(0xFF00FFD1)   // Vivid neon cyan — primary accent
val SignalTealDim   = Color(0xFF006E5A)   // Muted teal for borders/inactive
val WarnAmber       = Color(0xFFFFB84D)   // Warm amber caution lamp
val ErrorCoral      = Color(0xFFFF6B6B)   // Bright coral for faults
val BackgroundCharcoal = Color(0xFF080B12) // Near-black blue-black
val SurfaceCharcoal = Color(0xFF0E1219)   // Slightly lifted panel
val SurfaceRaised   = Color(0xFF141A26)   // Card / raised element
val OutlineSubtle   = Color(0xFF1E2636)   // Border / divider
val TextPrimary     = Color(0xFFE8F0ED)   // High-contrast white-green
val TextSecondary   = Color(0xFF7A8A95)   // Dimmed readout
val GridLine        = Color(0xFF1A2230)   // Oscilloscope grid lines
val SignalGlow      = Color(0x3300FFD1)   // 20% teal glow for fills
val HifiGold        = Color(0xFFFFC107)   // Golden brass HiFi accent
val CarbonBlack     = Color(0xFF040609)   // Carbon weave deep black
val McIntoshBlue    = Color(0xFF00A2FF)   // McIntosh scale backlit ice-blue
